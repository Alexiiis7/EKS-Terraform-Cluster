apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: default
  labels:
    app: database
spec:
  ports:
  - port: 3306
    name: mariadb-port
  clusterIP: None
  selector:
    app: database
